# JDK 8 home is resolved per-environment via the user-level ~/.gradle/gradle.properties
# (or JAVA_HOME). Hardcoding an absolute path here breaks cross-platform builds.
#org.gradle.java.home=/usr/lib/jvm/java-8-openjdk
