pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.minecraftforge.net/' }
    }
    repositories {
        maven { url = 'https://maven.parchmentmc.org' } // Add this line
    }
}

rootProject.name = 'coldsweat'
